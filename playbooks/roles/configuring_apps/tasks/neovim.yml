---
- name: Copy nvim config directory
  shell: "cp -rf {{ dotfiles_repo_path }}/nvim/HOME/.config $HOME/" 

- name: Run PlugInstall command in nvim to install plugins
  shell: "nvim -c PlugInstall -c qa"
